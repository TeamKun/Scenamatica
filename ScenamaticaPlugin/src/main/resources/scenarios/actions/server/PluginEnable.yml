scenamatica: ${project.version}

name: actions_server_plugin_enable
description: L6C2 in scenarios.xlsx
on:
  - type: manual_dispatch
scenario:
  - type: execute
    action: server_plugin_enable
    with:
      plugin: "LightHeadPlugin"
  - type: expect
    action: server_plugin_enable
    with:
      plugin: "LightHeadPlugin"
  - type: require
    action: server_plugin_enable
    with:
      plugin: "LightHeadPlugin"
