# noinspection YAMLSchemaValidation
scenamatica: ${project.version}

name: actions_server_log_4
description: Testing server_log action without argument or not
on:
  - type: on_load
  - type: manual_dispatch

scenario:
  - type: execute
    action: server_log
    with:
      message: "PONG"
      level: INFO
  - type: expect
    action: server_log
