# noinspection YAMLSchemaValidation
scenamatica: ${project.version}

name: actions_server_command_dispatch_4
description: Testing command_dispatch action without argument works or not
on:
  - type: on_load
  - type: manual_dispatch

context:
  actors:
    - name: Actor001


scenario:
  - type: execute
    action: command_dispatch
    with:
      command: say Hello World!
      sender: Actor001
  - type: expect
    action: command_dispatch
