# noinspection YAMLSchemaValidation
scenamatica: ${project.version}

name: actions_entity_damage_2
description: Testing entity_damage action with amount works or not
on:
  - type: on_load
  - type: manual_dispatch

context:
  actors:
    - name: Actor001

scenario:
  - type: execute
    action: entity_damage
    with:
      target: "@e"
      amount: 10
  - type: expect
    action: entity_damage
    with:
      target: "@e"
      amount: 10
    timeout: 20

