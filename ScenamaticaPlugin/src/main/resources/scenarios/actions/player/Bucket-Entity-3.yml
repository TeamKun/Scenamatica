# noinspection YAMLSchemaValidation
scenamatica: ${project.version}

name: actions_player_bucket_entity_3
description: Testing PlayerBucketEntityAction without entity works or not
on:
  - type: on_load
  - type: manual_dispatch

context:
  actors:
    - name: Actor001

scenario:
  - type: execute
    action: player_bucket_entity
    with:
      target: Actor001
      bucket:
        type: WATER_BUCKET
      entity:
        type: SALMON

  - type: expect
    action: player_bucket_entity
    with:
      target: Actor001
      bucket:
        type: WATER_BUCKET
      entityBucket:
        type: SALMON_BUCKET
