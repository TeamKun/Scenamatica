# noinspection YAMLSchemaValidation
scenamatica: ${project.version}

name: general_context_actor
description: Testing actor generation works or not
order: -100
on:
  - type: manual_dispatch
  - type: on_load

context:
  actors:
    - name: Actor001
      gamemode: SURVIVAL
      displayName: §c§lActor001

scenario: []
