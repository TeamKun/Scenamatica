# noinspection YAMLSchemaValidation
scenamatica: ${project.version}

name: actions_player_item_consume
description: Testing player_item_consume action works or not
on:
  - type: on_load
  - type: manual_dispatch

context:
  actors:
    - name: Actor001


scenario:
  - type: execute
    action: player_item_consume
    with:
      target: Actor001
      item:
        type: apple
  - type: expect
    action: player_item_consume
    with:
      target: Actor001
      item:
        type: apple

